# Devcontainer for agent-sandbox development
# Requires: run `./images/build.sh` first to build local images

ARG BASE_IMAGE=agent-sandbox-claude:local
FROM ${BASE_IMAGE}

# Any devcontainer-specific customizations can go here
